import auth from '@react-native-firebase/auth';  // Use React Native Firebase's auth module
import firestore from '@react-native-firebase/firestore'; // Use React Native Firebase's Firestore module

// No need to initialize Firebase with config, it's done automatically
export { auth, firestore };
